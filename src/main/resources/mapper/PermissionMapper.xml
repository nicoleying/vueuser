<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.dao.SysPermissionDao">
    <resultMap id="BaseResult" type="com.qf.domain.SysPermission">
        <id column="permId" property="permId"/>
        <result column="permName" property="permName"></result>
        <result column="permUrl" property="permUrl"></result>
        <result column="permState" property="permState"></result>
    </resultMap>
    <select id="findPermissionByUsername" resultMap="BaseResult" parameterType="string">
        select
    </select>
</mapper>